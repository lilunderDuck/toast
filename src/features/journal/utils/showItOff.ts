// import { annotate } from "rough-notation"
// import __style from './rough-notion-fix.module.css'

export function showItOff(target: string) {
  // const targetElement = document.querySelector<HTMLElement>(`[${target}]`)
  // const parentElement = targetElement?.parentElement
  // console.assert(target.startsWith('rough-'), `${target}: the attribute does not start with rough-`)
  // console.assert(targetElement, 'element does not exist')
  // console.log('found', targetElement)
  // console.log('parent element', parentElement)

  // parentElement?.classList.add(__style.rough)

  // const animation = annotate(targetElement!, {
  //   type: 'box',
  //   color: 'yellow',
  //   padding: 5
  // })

  // animation.show()

  // setTimeout(() => {
  //   animation.remove()
  //   parentElement?.classList.remove(__style.rough)
  // }, 3000)

  // animation.remove()
}